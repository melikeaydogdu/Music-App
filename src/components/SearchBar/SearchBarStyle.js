import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        backgroundColor: '#dcdcdc',
        padding: 2,
        margin: 2,
        borderRadius: 10,
    },
});